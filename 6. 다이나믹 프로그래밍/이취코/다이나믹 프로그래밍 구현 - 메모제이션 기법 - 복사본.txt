# 피보나치 수열 소스코드(재귀적) - 다이나믹 프로그래밍 - 메모제이션 기법 - 탑다운 방식(재귀함수 이용)
d = [0] * 100

def fibo(x):
    if x == 1 or x == 2:
        return 1
    if d[x] != 0:
        return d[x]
    d[x] = fibo(x - 1) + fibo(x - 2)
    return d[x]

print(fibo(99))