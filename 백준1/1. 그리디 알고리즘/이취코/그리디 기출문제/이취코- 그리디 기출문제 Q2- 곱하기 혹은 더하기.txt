data = list(map(int, input()))
length = len(data)

for i in range(1, length):
    data[i] = max(data[i] + data[i - 1], data[i] * data[i - 1])
    
print(data[length - 1])

#################################################

data = list(map(int, input()))
length = len(data)
result = data[0]
for i in range(1, length):
    if result == 0 or data[i] == 0:
        result += data[i]
    else:
        result *= data[i]

print(result)