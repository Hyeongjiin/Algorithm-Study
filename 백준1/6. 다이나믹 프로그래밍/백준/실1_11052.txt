import sys
input = sys.stdin.readline
N = int(input())
card = list(map(int, input().split()))
total = [0] * (N + 1)
for i in range(N):
    for j in range(N + 1):
        if i + 1 + j <= N:
            total[i + 1 + j] = max(total[i + 1 + j], total[j] + card[i])
print(max(total))