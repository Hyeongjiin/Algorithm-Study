import sys
import math

def b_search(data, half, start, end):
    while start <= end:
        total = 0
        mid = (start + end) // 2
        for i in range(N):
            for j in range(N):
                if data[i][j] > mid:
                    total += mid
                else:
                    total += data[i][j]
        if total >= half:
            end = mid - 1
            result = mid
        else:
            start = mid + 1
    return result

input = sys.stdin.readline
N = int(input())
data = []
for i in range(N):
    data.append(list(map(int, input().split())))
total = 0
maxi = 0
for i in range(N):
    for j in range(N):
        total += data[i][j]
        if data[i][j] > maxi:
            maxi = data[i][j]
half = math.ceil(total / 2)
start = 0
end = maxi
result = b_search(data, half, start, end)
print(result)





